package com.calmlauncher.navigation

object Routes {
    const val HOME = "home"
    const val APPS = "apps"
    const val SEARCH = "search"
    const val FOCUS = "focus"
    const val SETTINGS = "settings"
    const val TOOL = "tool/{name}"
    const val LAUNCH = "launch/{packageName}/{label}/{delaySeconds}/{confirm}/{blocked}/{deadEnd}"
}

