package com.calmlauncher.core.designsystem.theme

import androidx.compose.ui.unit.dp

/**
 * 8px base spacing scale from the Stitch design system. All vertical rhythm and
 * horizontal margins derive from these values.
 */
object Spacing {
    val stackSm = 4.dp
    val base = 8.dp
    val stackMd = 12.dp
    val gutter = 16.dp
    val marginMobile = 24.dp
    val stackLg = 32.dp

    /** Standard list-row vertical padding (Stitch `py-stack-md`). */
    val rowVertical = 12.dp

    /** Height of the bottom navigation bar (Stitch `h-16`). */
    val bottomNavHeight = 64.dp

    /** Divider thickness (0.5pt feel). */
    val divider = 1.dp
}
