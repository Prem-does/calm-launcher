<?xml version="1.0" encoding="utf-8"?>
<!--
    typeWindowsChanged is what makes split-screen and picture-in-picture enforceable. A window
    entering or leaving the screen does not necessarily produce a typeWindowStateChanged event for
    the app inside it, so without this the service never hears about a blocked app that appears in
    the half of the screen the user is not touching.

    flagRetrieveInteractiveWindows is required for AccessibilityService.getWindows() to return the
    full window list rather than only the active window — see FocusBlockAccessibilityService.

    notificationTimeout is halved to 50ms: it is the minimum gap between events of the same type,
    so it is also the worst-case delay before a blocked app is noticed. Blocking is supposed to feel
    immediate, and 100ms of coalescing is a visible head start.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:description="@string/focus_service_desc"
    android:notificationTimeout="50" />
